---
name: lecture-to-notes-os
description: '将操作系统（OS）课程课件 PDF 转换为结构化 Obsidian Markdown 笔记。含中文 OCR、内容提取、大纲规划、紧凑风格撰写。专为操作系统课程优化（进程调度、内存管理、文件系统、同步互斥等）。Use when: 处理 OS 课件 PDF、提取 OS 讲义文字、OCR 中文 OS 课件、撰写操作系统课程笔记。'
argument-hint: '[PDF文件名] [可选: 讲义编号]'
---

# OS 课件 PDF → 结构化笔记

将操作系统（Operating System）课程课件 PDF 转换为紧凑、信息密度高的 Obsidian Markdown 笔记。针对 OS 核心主题（进程管理、内存管理、文件系统、I/O、同步与死锁）进行了领域适配。

## 适用场景

- 处理操作系统课程课件 PDF，生成结构化笔记
- 课件包含大量图片（架构图、状态转换图、内存布局），需 OCR 提取中文文本
- 已有提取文本，需整理为规范 OS 笔记格式

## OS 课程核心主题

笔记内容通常覆盖以下模块，撰写时注意识别归属：

| 模块 | 核心内容 |
|------|---------|
| 进程管理 | PCB、状态转换、fork/exec、线程模型、协程 |
| 调度 | FCFS/SJF/RR/多级队列、实时调度、CFS |
| 同步与互斥 | 信号量、管程、条件变量、死锁（银行家算法） |
| 内存管理 | 分段/分页、虚拟内存、TLB、页面置换算法 |
| 文件系统 | inode、目录结构、VFS、磁盘调度 |
| I/O 与设备 | 中断、DMA、SPOOLing、设备驱动模型 |

## 阶段一：提取文本

### 判断 PDF 类型

先运行 `pdftotext` 试提取，观察输出量：

```bash
pdftotext -layout "XX讲义名.pdf" /tmp/lectureXX.txt
wc -l /tmp/lectureXX.txt
```

- **输出 > 100 行有效文本** → 文本型 PDF，直接使用提取结果，跳到阶段三
- **输出极少（< 50 行）** → 图片型 PDF，进入阶段二 OCR

### 备选：MarkItDown 转换

对文本型 PDF，MarkItDown 可能保留更好的公式和结构：

```bash
python3 -m markitdown "XX讲义名.pdf" > lectureXX_raw.md
```

## 阶段二：OCR 图片型 PDF

仅当 `pdftotext` 提取失败时使用。

### 2.1 安装依赖（一次性）

```bash
sudo apt-get install -y tesseract-ocr tesseract-ocr-chi-sim
```

### 2.2 转换 PDF 页面为图片

```bash
mkdir -p /tmp/lectureXX_pages
pdftoppm -r 300 "XX讲义名.pdf" /tmp/lectureXX_pages/page
```

### 2.3 批量 OCR

使用 Pylance RunCodeSnippet 工具批量处理，避免终端超时：

```python
import subprocess, glob, os

pages = sorted(glob.glob('/tmp/lectureXX_pages/page-*.ppm'))
# 若为 .png：glob.glob('/tmp/lectureXX_pages/page-*.png')

results = []
for p in pages:
    r = subprocess.run(
        ['tesseract', '-l', 'chi_sim+eng', p, 'stdout'],
        capture_output=True, text=True
    )
    results.append(f"=== {os.path.basename(p)} ===\n{r.stdout}")

with open('/tmp/lectureXX_ocr.txt', 'w') as f:
    f.write('\n'.join(results))
```

### 2.4 OCR 结果

OCR 输出可能有识别错误（中英混排、符号丢失），大模型应根据上下文推断修正，无需人工清理。

## 阶段三：理解与规划（动笔前必做）

拿到提取文本后，**不要直接写笔记**。先通读全文，建立对 OS 课件内容的整体理解。

### 3.1 梳理知识脉络

1. **模块识别**：课件属于 OS 的哪个模块？（进程管理 / 调度 / 同步 / 内存 / 文件 / I/O）
2. **章节划分**：课件自然分为几个大节？各节之间的逻辑关系（机制引出问题 → 解决方案 → 算法对比）
3. **核心要素**：列出课件中的核心概念、数据结构（PCB/inode/页表）、算法（调度/置换/分配）、机制（信号量/管程/中断）——这是笔记必须覆盖的骨架

### 3.2 确定笔记结构

在阅读理解的基础上，先写出笔记的**一级/二级标题大纲**（即章节结构），确认覆盖完整后再进入阶段四。

```markdown
# 第X章 主题名

## 一、问题与背景
## 二、核心机制
### 2.1 数据结构
### 2.2 算法流程
## 三、算法对比
## 四、经典问题
```

### 3.3 评估难点

- 哪些机制交互复杂（如 fork+exec 的地址空间变化），需要仔细推敲？
- 哪些算法有多种变体（如页面置换算法的 Belady 异常），需要对比？
- 课件中是否有图表（状态转换图、内存布局、调度甘特图）需要文字描述？

## 阶段四：撰写结构化笔记

### 4.1 笔记格式规范

笔记文件放入仓库根目录，命名 `XX-主题.md`（如 `03-进程调度.md`）。

#### YAML 前置元数据

```yaml
---
title: 主题中文名
tags:
  - OS
  - <模块标签（process/scheduling/memory/filesystem/sync/io）>
lecture: XX
---
```

#### 写作风格（严格遵循）

1. **机制/定义优先**：先写数据结构或核心定义，后跟最多一句 intuition
2. **无冗余**：删除历史轶事、"重要意义"、"广泛应用"、课程口号、励志内容、过渡套话
3. **算法/流程紧凑**：用伪代码或编号列表描述流程，不分步展开叙述；用 `→` 连接状态转换
4. **表格替代段落**：比较多个机制/算法/数据结构时用表格，不写对比段落
5. **示例内联**：`例：进程 P1(24ms), P2(3ms), P3(3ms) → RR(q=4) 周转时间: P1=30, P2=7, P3=10`
6. **图文转文字**：课件中的架构图、状态转换图、内存布局、时序图用简练文字概括其要点

#### 格式约定

- **数据结构**：`**PCB**：包含 pid, state, priority, context...`
- **机制/系统调用**：`` `fork()` ``：创建子进程，返回 pid
- **算法**：`**算法名**：描述。时间复杂度 $O(n)$`
- **状态转换**：`就绪 →(调度) 运行 →(时间片完) 就绪`，用 `→(原因)` 标注
- **数学**：`$...$` 行内、`$$...$$` 块级（如磁盘寻道时间公式）
- **代码/命令**：行内 `` `code` ``，多行用 ```` ``` ```` 代码块
- **引用**：Obsidian `[[wikilink]]` 格式
- **标签**：`#OS #scheduling`

### 4.2 OS 常见内容类型的写作模板

#### 机制/系统调用

```markdown
**`fork()`**：创建子进程，子进程返回 `0`，父进程返回子进程 pid。
> 原理：复制父进程地址空间（COW 优化），共享打开文件。
> 例：`pid = fork(); if (pid == 0) { /* child */ }`
```

#### 算法流程

```markdown
**RR 调度**（时间片轮转）：
1. 维护就绪队列（FIFO）
2. 当前进程用完时间片 → 插入队尾，调度队首
3. 进程阻塞 → 从队列移除，唤醒后重新入队
> 时间片选择：太小 → 上下文切换开销大；太大 → 退化为 FCFS
```

#### 算法对比表

```markdown
| 算法 | 决策依据 | 抢占 | 饥饿可能 | 特点 |
|------|---------|------|---------|------|
| FCFS | 到达顺序 | 否 | 否 | 护航效应 |
| SJF | 最短 CPU 区间 | 可选 | 是 | 最优平均等待 |
| RR | 时间片 | 是 | 否 | 响应时间好 |
| Priority | 优先级 | 是 | 是 | 老化可解饥饿 |
```

#### 状态转换

```markdown
**进程状态转换**：
`new →(admit) ready →(dispatch) running →(exit) terminated`
`running →(interrupt) ready`
`running →(I/O wait) waiting →(I/O done) ready`
```

#### 关键问题对比

```markdown
| | 进程 | 线程 |
|---|------|------|
| 资源 | 独立地址空间 | 共享地址空间 |
| 切换开销 | 大（TLB flush） | 小 |
| 通信 | IPC（pipe/mq/shm） | 直接读写共享变量 |
| 创建 | `fork()` | `pthread_create()` |
```

### 4.3 笔记结构模板

```markdown
# 第X章 主题名

## 一、问题引入

**核心问题**：一句话描述要解决什么问题。

---

## 二、核心机制

### 2.1 数据结构

**XXX 结构**：字段1, 字段2, ...

### 2.2 算法/流程

**算法名**：
1. 步骤一
2. 步骤二
...

---

## 三、算法/机制对比

| 机制 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| ... | ... | ... | ... |

---

## 四、经典问题

**问题名**：描述 + 例。
```

### 4.4 质量检查

完成后检查：
- [ ] YAML frontmatter 完整（title, tags, lecture）
- [ ] 原文中的核心概念（数据结构、算法、机制、系统调用）是否全部覆盖
- [ ] 是否有冗余叙述（口头禅、"重要"、重复解释、过渡套话）
- [ ] 算法/流程是否压缩到编号列表或一段
- [ ] 表格是否替代了长段落（对比表、特性表）
- [ ] 示例是否内联
- [ ] 架构图/状态图/内存布局是否用文字概括
- [ ] 代码片段/伪代码是否正确
- [ ] 机制交互关系是否明确（如 fork+exec、信号量+管程）
