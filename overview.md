# 操作系统课程笔记大纲

## 模块一：系统概览

### 第1章 计算机系统概述（`OS-CH-1`）
- 1.1 计算机系统组成：CPU、内存、I/O、总线
- 1.2 存储层次：寄存器 → Cache → 主存 → 外存
- 1.3 取指-译码-执行周期
- 1.4 中断机制概述
- 1.5 多处理器与多核架构

### 第2章 操作系统结构（`OS-CH-2`）
- 2.1 操作系统服务：程序执行、I/O 操作、文件系统、通信、错误检测
- 2.2 用户接口：CLI、GUI、系统调用
- 2.3 系统调用实现：用户态 ↔ 内核态切换
- 2.4 OS 设计结构：单体、分层、微内核、模块化、混合
- 2.5 OS 发展史：批处理 → 多道程序 → 分时系统

### 第3章 中断和异常（`OS-CH-3`）
- 3.1 中断 vs 异常：异步 vs 同步
- 3.2 中断分类：可屏蔽 / 不可屏蔽
- 3.3 异常分类：Fault / Trap / Abort
- 3.4 中断处理流程：保存上下文 → 查中断向量表 → 执行 ISR → 恢复
- 3.5 Linux 中断处理：Top Half / Bottom Half（tasklet, workqueue, 内核线程）
- 3.6 中断共享与 IRQ

---

## 模块二：进程与线程

### 第4章 进程（`OS-CH-4-进程-1` + `OS-CH-4-进程-2`）
- 4.1 进程概念：程序 vs 进程；PCB 结构
- 4.2 进程状态转换：new → ready → running → waiting → terminated
- 4.3 进程创建与终止：`fork()`, `exec()`, `exit()`, `wait()`, `abort()`
- 4.4 僵尸进程 & 孤儿进程 & 级联终止
- 4.5 上下文切换
- 4.6 进程间通信（IPC）
  - 共享内存：生产者-消费者、有界缓冲区
  - 消息传递：`send()`/`receive()`、直接/间接通信、同步/异步
- 4.7 客户机-服务器通信：Socket、RPC、管道
- 4.8 Chrome 多进程架构

### 第5章 线程（`OS-CH-5`）
- 5.1 线程概念：调度的基本单元；TCB
- 5.2 进程 vs 线程：资源所有权 vs 调度/执行
- 5.3 线程模型：用户级线程、内核级线程、多对一/一对一/多对多
- 5.4 线程库：Pthreads、Windows Threads、Java Threads
- 5.5 隐式线程：线程池、OpenMP
- 5.6 多线程编程问题：`fork()`/`exec()` 语义、信号处理、线程取消、TLS

---

## 模块三：CPU 调度

### 第6章 进程调度（`OS-CH-6`）
- 6.1 调度层次：长程 / 中程 / 短程调度
- 6.2 CPU-I/O 执行周期
- 6.3 调度准则：CPU 利用率、吞吐量、周转时间、等待时间、响应时间
- 6.4 调度算法
  - FCFS（护航效应）
  - SJF / SRTF（最优平均等待）
  - Priority Scheduling（饥饿 + 老化）
  - RR（时间片选择）
  - 多级队列 & 多级反馈队列
- 6.5 线程调度：进程竞争范围 vs 系统竞争范围
- 6.6 多处理器调度：负载均衡、处理器亲和性
- 6.7 实时调度：RMS、EDF
- 6.8 算法评估：确定性建模、排队模型、仿真
- 6.9 实际系统案例：Linux CFS、Windows、Solaris

---

## 模块四：同步与死锁

### 第7章 同步（`OS-CH-7`）
- 7.1 临界区问题 & 竞争条件
- 7.2 Peterson 解决方案
- 7.3 硬件同步：Test-and-Set、Compare-and-Swap、原子变量
- 7.4 互斥锁（Mutex）
- 7.5 信号量：`wait()`/`signal()`、计数信号量、二进制信号量
- 7.6 管程：条件变量、`wait()`/`signal()`
- 7.7 Liveness（活性）：死锁、饥饿、优先级反转
- 7.8 经典同步问题
  - 生产者-消费者（有界缓冲区）
  - 读者-写者问题
  - 哲学家就餐问题
- 7.9 实际系统同步实现：Linux、Windows、Solaris

### 第8章 死锁（`OS-CH-8`）
- 8.1 死锁定义：永久阻塞、资源竞争
- 8.2 死锁四个必要条件：互斥、持有并等待、不可抢占、循环等待
- 8.3 资源分配图
- 8.4 死锁预防：破坏四个条件之一
- 8.5 死锁避免：银行家算法、安全状态
- 8.6 死锁检测与恢复：等待图、进程终止、资源抢占
- 8.7 综合死锁解决策略

---

## 模块五：内存管理

### 第9章 物理内存管理（`OS-CH-9`）
- 9.1 地址绑定：编译时 / 加载时 / 执行时
- 9.2 逻辑地址 vs 物理地址；MMU
- 9.3 连续内存分配
  - 固定分区 vs 动态分区
  - 动态分配算法：First-fit / Best-fit / Worst-fit
  - 外部碎片 & 内部碎片 & 紧凑
- 9.4 分段：段表、段基址 + 界限
- 9.5 分页：页表、页号 + 页内偏移
  - 页表结构：分层页表、哈希页表、倒置页表
  - TLB（快表）
- 9.6 交换（Swapping）
- 9.7 动态链接与共享库

### 第10章 虚拟内存管理（`OS-CH-10`）
- 10.1 虚拟内存定义 & 优势
- 10.2 请求调页（Demand Paging）：有效-无效位、缺页中断
- 10.3 写入时复制（Copy-on-Write）
- 10.4 页面置换算法
  - FIFO（Belady 异常）
  - OPT（最优置换，理论参照）
  - LRU / LRU 近似（引用位、二次机会、Clock）
  - 计数算法：LFU / MFU
- 10.5 页帧分配：固定分配 vs 可变分配；局部置换 vs 全局置换
- 10.6 系统颠簸（Thrashing）：工作集模型、页故障频率（PFF）
- 10.7 内存映射文件 & 共享内存
- 10.8 内核内存管理：Buddy System、Slab 分配

---

## 模块六：存储与 I/O

### 第11章 大容量存储（`OS-CH-11`）
- 11.1 磁盘物理结构：磁道、柱面、扇区
- 11.2 磁盘性能参数：寻道时间、旋转延迟、传输时间
- 11.3 磁盘调度算法：FCFS / SSTF / SCAN / C-SCAN / LOOK / C-LOOK
- 11.4 磁盘格式化 & 坏块管理
- 11.5 RAID：RAID 0/1/4/5/6/10、镜像、奇偶校验
- 11.6 SSD（固态硬盘）：FTL、磨损均衡、垃圾回收
- 11.7 存储层次 & RAM 盘

### 第12章 I/O 系统（`OS-CH-12`）
- 12.1 I/O 硬件：端口、总线、设备控制器
- 12.2 I/O 方式：轮询（Polling）、中断驱动 I/O、DMA
- 12.3 应用程序 I/O 接口：块设备 / 字符设备 / 网络设备 / 时钟
- 12.4 阻塞 vs 非阻塞 I/O、异步 I/O、向量 I/O
- 12.5 内核 I/O 子系统：调度、缓冲、缓存、假脱机
- 12.6 错误处理 & I/O 保护
- 12.7 流式 I/O（STREAMS）
- 12.8 电源管理

---

## 模块七：文件系统

### 第13章 文件系统接口（`OS-CH-13`）
- 13.1 文件概念 & 属性（名称、标识符、类型、大小、保护、时间）
- 13.2 文件操作：`create`/`open`/`read`/`write`/`seek`/`delete`/`truncate`
- 13.3 打开文件表：文件指针、打开计数、磁盘位置
- 13.4 文件锁：共享锁（读者锁）& 独占锁（写者锁）
- 13.5 文件类型 & 结构：字节流、记录序列、树
- 13.6 访问方法：顺序访问、直接访问、索引访问
- 13.7 目录结构：单级 / 两级 / 树形 / 无环图 / 通用图
- 13.8 文件系统安装（Mount）
- 13.9 文件共享：多用户、远程文件系统、一致性语义（Unix 语义）
- 13.10 文件保护：访问控制列表（ACL）、所有者/组/其他

### 第14章 文件系统实现（`OS-CH-14`）
- 14.1 文件系统分层结构
- 14.2 文件系统数据结构
  - 磁盘：引导控制块、卷控制块、目录、FCB/inode
  - 内存：安装表、目录缓存、系统级打开文件表、进程级打开文件表
- 14.3 虚拟文件系统（VFS）：统一接口
- 14.4 目录实现：线性列表、哈希表
- 14.5 分配方法：连续分配、链接分配、索引分配（inode）
- 14.6 空闲空间管理：位图、空闲链表、分组、计数
- 14.7 效率与性能：缓存、预读、延迟写
- 14.8 恢复：一致性检查、日志（Journaling）
- 14.9 实际文件系统：ext3/ext4、UFS、NTFS
